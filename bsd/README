This directory contains code specific to BSD systems.  It is not actually used,
so if you are porting Quake II to a BSD system then use this code as a
reference and merge it all into ../src

Send any patches to quake2-devel@lists.quakeforge.net
