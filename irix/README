This directory contains code specific to IRIX systems.  It is not actually used,
so if you are porting Quake II to an IRIX system then use this code as a
reference and merge it all into ../src

Send any patches to quake2-devel@lists.quakeforge.net
